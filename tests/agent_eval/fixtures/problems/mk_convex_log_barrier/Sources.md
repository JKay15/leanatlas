# Sources

This problem fixture is part of the **mentor keyword classics** agent-eval suite.

**Reference IDs** below resolve to full bibliographic entries (with stable URLs) in:

- `docs/references/mentor_keywords.yaml`

## References
- `TOPSOE_LOGBNDS` — Flemming Topsøe, *Some bounds for the logarithmic function*, Section 1, inequality (1): 1 − 1/x ≤ ln x ≤ x − 1 (x>0).
- `BV_CVXBOOK_2004` — Boyd & Vandenberghe, *Convex Optimization* (2004), Chapter 11: barrier methods / interior-point context for log barriers.
- `MATHLIB_LOG_BASIC` — mathlib4 docs: `Real.log_le_sub_one_of_pos` (formal lemma used in Lean).
