-- Scratch.lean for mk_poly_factorization_square

-- You may use `sorry` here. Never import Scratch from Proof/Cache.

import Problems.mk_poly_factorization_square.Spec

namespace Problems.mk_poly_factorization_square

open Polynomial

-- Scratch: `ring` is often enough.
theorem scratch_try (a : ℝ) : Goal a := by
  simp [Goal]
  ring

end Problems.mk_poly_factorization_square
