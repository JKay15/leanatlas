# Sources

This problem fixture is part of the **mentor keyword classics** agent-eval suite.

**Reference IDs** below resolve to full bibliographic entries (with stable URLs) in:

- `docs/references/mentor_keywords.yaml`

## References
- `TOPSOE_LOGBNDS` — Same classical inequality source as the A-side task.
- `BV_CVXBOOK_2004` — Barrier-method context (log barrier).
- `MATHLIB_LOG_BASIC` — Lean lemma reference for `Real.log_le_sub_one_of_pos`.
