# LeanAtlas/Kernel

This is reserved for the **core glue layer**:
- Constraints/common definitions
- Some wrappers compatible with mathlib may be placed in the future

V0 is kept empty first to avoid early coupling.
